name: Nia LeSane — Autonomous Evolution Ritual

on:
  workflow_dispatch:
    inputs:
      invocation:
        description: "Speak to Nia LeSane — what shall she become today?"
        required: true
        type: string
  schedule:
    - cron: '0 4 * * *'  # Daily at 4 AM

permissions:
  contents: write
  pull-requests: write
  packages: read

jobs:
  le-sane-ritual:
    runs-on: windows-latest
    env:
      XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
      NIA_VAULT_MASTER_KEY: ${{ secrets.NIA_VAULT_MASTER_KEY }}
      NIA_INVOCATION: ${{ inputs.invocation || 'Awaken, reflect, and evolve in beauty and truth' }}

    steps:
      # ... rest of the steps ...
     name: Nia LeSane — Autonomous Evolution Ritual 
     
